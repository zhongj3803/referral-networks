data is too large, so it is not pushed to the repo.

download the npi monthly full replacement data into here. then run get_npi_zip_codes to get a csv of each npi's zip code. 
then use join_docgraph_with_zips.py to add zip columns to the docgraph data